
name = "tammy-tech-news-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# Store your Gemini API key as a secret (free — never put it in this file).
# Run this once after deploying:
#   npx wrangler secret put GEMINI_API_KEY
